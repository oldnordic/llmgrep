---
phase: 18-sqlite-backend-refactor
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/query.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "search_calls_impl() function exists taking &Connection parameter"
    - "search_calls_impl() contains verbatim SQL query logic from search_calls()"
    - "query.rs::search_calls() calls search_calls_impl() as wrapper"
    - "Code compiles after _impl() function creation"
  artifacts:
    - path: "src/query.rs"
      provides: "search_calls_impl() internal function for search_calls logic"
      exports: ["search_calls_impl"]
      covered_by: "Task 1"
  key_links:
    - from: "src/query.rs::search_calls"
      to: "src/query.rs::search_calls_impl"
      via: "function call wrapper"
      pattern: "search_calls_impl\\(conn, options\\)"
---

<objective>
Create search_calls_impl() internal function that takes &Connection parameter, containing the core search_calls SQL query logic.

Purpose: Separate "how to query" (internal implementation with explicit Connection) from "where to get connection" (trait method with &self.conn).
Output: search_calls_impl() function in query.rs with verbatim search_calls logic.
</objective>

<execution_context>
@/home/feanor/.claude/get-shit-done/workflows/execute-plan.md
@/home/feanor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/018-sqlite-backend-refactor/18-01-SUMMARY.md
@.planning/phases/018-sqlite-backend-refactor/18-02-SUMMARY.md
@.planning/phases/018-sqlite-backend-refactor/18-03-SUMMARY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/query.rs
@src/backend/sqlite.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create search_calls_impl() with Connection parameter</name>
  <files>src/query.rs</files>
  <action>
    1. Create new public(crate) function `search_calls_impl()` in query.rs:
       ```rust
       pub(crate) fn search_calls_impl(
           conn: &Connection,
           options: &SearchOptions,
       ) -> Result<(CallSearchResponse, bool), LlmError>
       ```

    2. Copy the ENTIRE body of `search_calls()` (lines ~1194-end of function, approximately 200-300 lines) into `search_calls_impl()`.

    3. Make the following modifications to the copied code:
       - Remove the `Connection::open_with_flags()` call at the beginning (conn is now a parameter)
       - Remove the database validation query (conn is already valid)
       - Keep all other logic verbatim

    4. Update the original `search_calls()` function to be a wrapper (same pattern as search_symbols and search_references):
       ```rust
       pub fn search_calls(options: SearchOptions) -> Result<(CallSearchResponse, bool), LlmError> {
           // Connection opening and validation (keep existing code pattern)
           let conn = match Connection::open_with_flags(options.db_path, OpenFlags::SQLITE_OPEN_READ_ONLY) {
               // ... same error handling as search_symbols ...
           };

           // Validation
           conn.query_row("SELECT name FROM sqlite_master WHERE type='table' LIMIT 1", [], |_| Ok(()))
               .map_err(|e| /* same error handling */)?;

           // Call the implementation
           search_calls_impl(&conn, &options)
       }
       ```

    5. Verify no logic changes: The SQL queries, scoring, filtering, and post-processing must be identical.
  </action>
  <verify>cargo test --lib search_tests::search_calls 2>&1 | tail -20</verify>
  <done>search_calls_impl() exists with Connection parameter, search_calls() is wrapper, tests pass</done>
</task>

</tasks>

<verification>
1. search_calls_impl() function exists in query.rs with signature (conn: &Connection, options: &SearchOptions)
2. search_calls() function calls search_calls_impl(&conn, &options)
3. All search_calls unit tests pass
4. SQL query logic is verbatim from original search_calls()
</verification>

<success_criteria>
- [ ] search_calls_impl() created with Connection parameter
- [ ] search_calls() wrapper maintains test compatibility
- [ ] All existing search_calls tests pass
- [ ] No logic changes (verbatim SQL copy)
</success_criteria>

<output>
After completion, create `.planning/phases/018-sqlite-backend-refactor/18-04-SUMMARY.md`
</output>
